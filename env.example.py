# Copy this file to `env.py` and customize

stela_db_root_path = '/Users/parke/Google Drive/Research/STELa/data'
stela_scratch_path = '/Users/parke/Google Drive/Research/STELa/scratch'

# helpful when using some stistools scripts that require moving to the
# directory where the data so that the user can move back to the code
# directory so that later imports or reloads work
stage1_code_path = '/Users/parke/Repos/STELa/stage1'